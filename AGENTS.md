# Sentinels Design Lab Repository

This is a standalone React/Vite application. Keep the frontend portable and provider-neutral. Add Firebase only when persistent data, authentication, storage, or server-side workflows are required. Do not introduce proprietary builder runtime dependencies.

Validate changes with `npm run lint`, `npm run typecheck`, and `npm run build`.